apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: plm-cloud
spec:
  rules:
    - host: plm.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend
                port:
                  number: 3000